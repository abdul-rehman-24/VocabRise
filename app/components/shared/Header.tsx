// app/components/shared/Header.tsx - Top navigation bar with branding

export default function Header() {
  return (
    <header className="bg-white border-b border-gray-200">
      {/* Logo/branding, navigation menu, user menu (avatar + dropdown) */}
    </header>
  )
}
