# Suppress deprecation warnings for non-critical dependencies
# The url.parse() deprecation in follow-redirects and openid-client 
# is from dependencies that haven't updated yet. This is safe to suppress.
